<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Device lock. USE_BIOMETRIC covers fingerprint and face on API 28+; the older
         USE_FINGERPRINT keeps API 23-27 working, where BiometricPrompt falls back to the
         legacy fingerprint API. Neither grants access to biometric data — they only allow
         asking the system to confirm the user is present. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- In-app update: install a downloaded APK from within the app. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Coarse only. LocZ resolves a city or a pincode, never a doorstep, and asking for
         fine location would cost permission grants for accuracy the product never uses. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 13+ requires an explicit grant before any notification is shown. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Backup is off because this app stores a signed-in session.

         Android's auto-backup is on by default and would copy the app's data directory to
         the user's Drive, and `adb backup` would pull the same data off a developer-enabled
         handset. flutter_secure_storage keeps refresh tokens in EncryptedSharedPreferences,
         whose key lives in the Keystore and does not leave the device — so a restored backup
         yields ciphertext nobody can read, and the app would sign the user out anyway.

         Turning it off removes the ambiguity rather than relying on that reasoning holding
         for every value the app ever stores. The cost is that a user replacing their phone
         signs in again, which is one OTP. -->
    <application
        android:label="LocZ"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Verified web links keep shared listing URLs in one namespace. Android
                 verifies ownership once assetlinks.json is deployed for the upload key. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="locz.in"
                    android:pathPrefix="/ad/" />
            </intent-filter>
            <!-- Custom-scheme fallback for campaigns and development tooling. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="locz"
                    android:host="ad" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
